# 闲鱼真机锚点契约（维护动作 + 订单同步）— 20260915.1

冻结人：Root/总控。勘察设备 OnePlus 9R `b0644fb5`（1080x2400，Android 14，闲鱼 Flutter 容器）。
证据：`artifacts/tasks/P09-unknown/xianyu-maintenance-20260915/recon-20260915-2/`（01–11 号 png/xml，文件名与本文锚点编号对应）。
语义冲突以本文为准；坐标均为 1080x2400 像素。**布局区（今日数据卡/横幅）可有可无，一切卡片类坐标使用前必须过 §0 守卫。**

## §0 布局守卫（所有列表页坐标动作前置）

- 进入列表页后先验证 tabs 语义节点存在且 bounds 与本文一致（±40px），不一致 → `LAYOUT_GUARD_FAILED` 安全停，禁止按旧坐标点击。
- 卡片区起点由 tabs 底边推导（tabs 底 = 885），**禁止硬编码绝对 y**；今日数据卡 [24,225][666,729] 与右侧推广位 [666,225][1080,729] 只作漂移诊断用。
- 顶部「横幅通知」（订单页实测 y 379..487）可有可无：订单容器锚点用 desc 前缀识别（§3），不受横幅影响。

## §1 我发布的页（列表管理）

- tabs（均可点击，content-desc「第 N 个标签，共 3 个」）：在卖 [0,753][219,885]（文本「187\n在卖」）、草稿 [219,753][387,885]、已下架 [387,753][603,885]。
- 在卖卡片：文本节点完整暴露（下架原因位无；含 托管/降价/编辑/诊断 + 标题 + 曝光N/浏览N/想要N + ¥价格）。卡片高度不均匀（实测 549/588/378），**cardIndex→y 公式不可靠**。
- 已下架卡片：文本暴露 下架原因（售出下架/主动下架）+ 删除/重新上架 + 标题 + 浏览N + ¥价格；首卡起点 = tabs 底（实测 885，旧记录 675/460 均为横幅在场变体）。
- **商品身份映射（platformItemId 定位）**：以卡片文本中的标题段做 titleContains 匹配定位目标卡；标题在无障碍树可读（在卖/已下架均确认）。这是替代 cardIndex 的主路径。
- 草稿 tab：本轮实测为空（「你还没有草稿」[0,885][1080,1728]），**坐标未标定**，待有草稿后补勘。

## §2 商品详情页 + 管理菜单（维护动作主路径，冻结）

- 详情页语义可点击节点：返回 [54,146][99,191]、分享按钮 [954,133][1026,205]、评论按钮、收藏按钮、**管理按钮 [843,2208][1044,2328]**（content-desc='管理按钮'，中心 (943,2268)；旧坐标 (936,2245) 在节点内，但后续一律用语义锚点）。
- 管理菜单（动作面板，全文本标签、均可点击、x 全宽 48..1032）：
  | 项 | bounds | 说明 |
  |---|---|---|
  | 推广宝贝 | [48,996][1032,1164] | **G3 付费，编排永不触碰** |
  | 超级擦亮 | [48,1164][1032,1332] | **G3 付费，编排永不触碰** |
  | 智能回复设置 | [48,1332][1032,1500] | 不用 |
  | 编辑 | [48,1500][1032,1668] | 备用 |
  | 下架 | [48,1668][1032,1836] | 文本锚点「下架」 |
  | 删除 | [48,1836][1032,2004] | 文本锚点「删除」 |
  | 取消 | [48,2004][1032,2172] | 安全退出 |
- 菜单项命中=文本精确匹配（等于「下架」/「删除」/「取消」）+ bounds 校验；菜单出现异常 → 点取消/BACK，零副作用退出。
- **批量下架/删除路径改为**：列表(标题定位) → 卡片进入详情 → 管理按钮 → 菜单文本锚点 → 动作 → 确认弹窗（沿用既有 GATED 确认流）→ BACK 回列表 → 下一项。放弃 cardIndex 坐标直击路径。

## §3 订单列表页（order-sync 用，冻结）

- 入口（我的页文本节点）：我卖出的 [467,957][611,1005]（中心 539,981）、我买到的 [673,957][817,1005]（中心 745,981）。
- 列表页结构：状态 tabs 全部/待付款/待发货/待收货/待评价/退款中（y 235..355，语义 desc「第 N 个标签，共 6 个」，readOrders 不操作）；顶部可能有横幅通知（379..487）。
- **行识别**：content-desc 前缀「订单信息」的节点为订单行（实测行高约 721px，SOLD/BOUGHT 同构）。
- 行内字段（顺序固定）：首行左=对手昵称（SOLD=买家/BOUGHT=卖家）、首行右=状态原文、第二块=商品标题、右侧=价格。**价格碎片化**：零宽空格 U+200B 切成多段（'¥'+'1'+'0'+'.8 0'），解析=去 \u200b 与空白拼接、去 ¥、十进制转分。
- **无订单号**（实测确认）：order_key 裁决见 `order-sync-20260915` 补遗 20260915.2——复合自然键 `{direction}|{对手昵称}|{标题截64}|{amount_cents}` 整体截 128；三段全缺 → NO_KEY。
- 订单页右上「管理」[936,103][1068,235] 为平台自有批量管理入口，本轮不使用。

## §4 翻转纪律

- 本契约只冻结锚点；`verified` 翻转仍按 order-sync §7：总控装机烟测通过后在迁移分支统一翻转，子代理不得自行翻转。
