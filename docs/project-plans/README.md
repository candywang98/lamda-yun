# 项目计划快照

本目录保存项目外层目录的计划与交接文件，便于从 GitHub 获取完整的开发上下文。文件内容与源文件一致，SHA-256 见 `snapshot-manifest.json`。

## 当前计划

权威活动计划是 [lamda_yun_一期计划_多智能体版.xlsx](lamda_yun_一期计划_多智能体版.xlsx)（任务状态以该表 `01_任务总表` 为准）。验收与范围基线仍看 [lamda_yun_一期计划_智能体执行版.xlsx](lamda_yun_一期计划_智能体执行版.xlsx)。两表均已同步到 2026-09-16。

当前为 P00–P49 共 50 个功能包；验收口径是 OnePlus 9R `b0644fb5` 单机分阶段，多机及 Note9 为后续扩展，不得标已通过。模块通过不代表整包真实验收。

优先阅读多智能体表的 `00_使用总览` / `01_任务总表` / `11_执行记录`，以及执行版的 `01_闭环计划`、`09_执行记录`、`10_当前包交接模板`。

截至 2026-09-16 的已验收切片：P14 单机签名版本生命周期、P28 闲鱼单件真实发布（Q03）、P41 订单同步 slice1、IM 聚合真机（原二期提前）。P08 同任务恢复为核心通过。P09 删除 happy path 未过（UNKNOWN/KEEP_WAITING，严禁重试）；P29/P30 批量动作待稳健定位；P10/P11 投屏远控第一片待 Q04 真机。

本目录是外层文件的版本快照，不会自动同步。`docs/phase1/multi-agent-work-items.json` 的 authority 相对路径仍指向原本地目录布局中的外层主计划；单独克隆本仓库时，可在本目录查阅对应快照。

## 历史资料

其余表格、实施手册、优化问题清单和交接说明用于追溯。旧文档中的完成比例、设备状态及待办事项可能已被新计划或较新的验收记录取代。

## 上传边界

源码、测试、迁移、依赖锁文件、开发文档和任务 Markdown 报告纳入版本管理。新增原始调试数据、设备数据库、APK、密钥、依赖目录和构建缓存不上传；任务报告中引用的原始证据可能仅在本地可用。原已跟踪的本地数据库、实验设备配置、TypeScript 构建缓存和浏览器 trace/清理备份从本次快照中移除，电脑上的原文件保留。本次不改写既有 Git 历史。
