# F15 software evidence summary

- Task: 抖音视频发布缺口专项
- Audited: 2026-09-24 (Asia/Shanghai)
- Software status: `SOFTWARE_DONE`
- Acceptance status: `NOT_RUN`
- Device acceptance: 未执行；未安装 APK、未触屏、未上传视频、未发布作品

## Delivered

- `mobile/companion/app/src/main/java/com/company/cloudctl/companion/features/douyin/publish/`
  - `DouyinMediaProfile.kt`: 视频 fileId、时长、容器格式、图库 cell、封面和文案的 fail-closed 输入模型。
  - `DouyinPrePublishPlanner.kt`: 14 步发布前声明式链路；顺序冻结为启动、首页/相机稳定、相册与媒体页、cellmark 选视频、裁剪返回、发布页、封面、标题/描述回读、发布前检查；随后停在 `WAITING_USER_BEFORE_PUBLISH` 人工检查点。
  - `DouyinPublishSteps.kt`: 复用当前已验证的 `dy_*` 定位器；选择视频只引用 `dy_gallery_cell_N` 与 `dy_cellmark_probe`，不使用图像中心坐标。上传完成、发布页就绪、平台审核中和成功使用独立 wire 状态，只有 `published` 可计为成功。
  - `BoundedUploadWait.kt`: 每阶段单调进展和停滞预算；有效进展复位连续停滞计数；未知结果和一次 `commit_once` 预留跨等待重试粘滞，禁止自动重发。
- `tests/integration/test_douyin_publish_delta.py`: 隔离内存 API 验证 Douyin steps shape 仍只存在协议注册面，生产 CommandV1 铸造面拒绝 `douyin.*`，不生成或派发真实任务。

## Verification

- `cd mobile/companion && ./gradlew --offline testDebugUnitTest lint assembleDebug`: `BUILD SUCCESSFUL`
- F15 Android 定向单测：`15 tests`, `0 failures`, `0 errors`
- `PATH="$PWD/.venv/bin:$PATH" python -m pytest -q tests/integration/test_douyin_publish_delta.py`: `2 passed`
- `PATH="$PWD/.venv/bin:$PATH" make verify`: exit 0; 全仓 `1153 passed, 11 skipped`，Studio `16 passed`，Web `311 passed`，Ruff/Pyright/mypy/typecheck 全通过
- `python scripts/plan_guard.py docs/current/tasks.json`: `valid: true`
- `git diff --check`: exit 0

## Remaining acceptance gap

- 未完成真实授权测试视频、视频/封面素材归属证明、设备侧实际上传等待回读、发布目标身份链、平台审核结果查询和独立复核。
- 当前未把 F15 新 planner 接入真实设备执行器，也未注册新的 CommandV1；这保持现有 open-only 与禁止真实发送边界。
- OnePlus Companion 崩溃、VOG 无障碍未启用、ELE 缺 Companion、设备锁和物理空闲确认未解决前，不进行真机写入。
