# F16 — 2026-09-24 隔离软件复核

## 结论与边界

- 基线：`092c6983e2ef85cb6f7d443b5f34a73353974d17`，分支 `integration/goal-20260924-no-send`。
- 开发状态：`IN_PROGRESS`；整体验收：`NOT_RUN`。本记录不是公众号真实发布验收，也不是部署许可。
- 最终定向测试：**58 passed，0 failed / errors / skipped**（原有测试 14 项，delta 测试 44 项）。
- 四个修改的 Python 文件 Ruff lint / format 检查通过；两个源码文件 Pyright 0 errors、mypy no issues。
- `plan_guard.py` exit 0；仅 F16 任务条目变化，其他任务状态未提升。`git diff --check` exit 0。
- 所有业务测试使用 fake transport、内存数据库及隔离 SQLite 迁移库。没有真实公众号 token、草稿、素材上传、发布调用；没有生产部署、设备操作或发送。联网仅用于只读获取公开官方文档。

## 实际修复

1. 请求层将 `access_token` 放入 URL 查询参数，对查询值进行编码；将内部 `thumbMediaId` 转换为官方 `thumb_media_id`，不改变冻结原文档。
2. 非成功 HTTP 不得误判成功；拒绝布尔、非整数及非正值 token TTL。
3. 提交前及获取 token 后再次核对 READY 草稿、media ID、账户有效性和归属、内容哈希、冻结参数及 INTENT 状态。并发 token 错误不能覆盖已消费的提交结果。
4. 修复 `freepublish/get` 把状态误当字符串的缺陷：按数字状态解析，校验返回的 `publish_id`，成功需有非空文章 URL。拒绝布尔、字符串、未知状态、缺失身份及成功证据不足的响应。
5. 对账结束前重新核对发布身份；较晚返回的查询或官方错误不能覆盖另一查询已经写入的终态。成功后删除/封禁使用明确错误码保留语义，提交次数仍为 1，不允许重发。
6. 修正旧测试错误的字符串响应，新增状态枚举、畸形响应、目标漂移和并发终态保护回归；测试假凭据运行时生成，不关闭安全检查。

## 可复现命令

从 `cloudctl-source` 运行；scratch 根由当前会话提供，所有临时结果保持在 scratch。

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest -q tests/integration/test_wechat_publisher.py tests/integration/test_wechat_publisher_delta.py --tb=short -p no:cacheprovider --basetemp "$PI_SCRATCH_DIR/goal-20260924/f16/acceptance/pytest" --junitxml "$PI_SCRATCH_DIR/goal-20260924/f16/acceptance/tests.xml"
.venv/bin/ruff check --no-cache services/control-api/src/cloudctl_api/wechat_client.py services/control-api/src/cloudctl_api/wechat_service.py tests/integration/test_wechat_publisher.py tests/integration/test_wechat_publisher_delta.py
.venv/bin/ruff format --check --no-cache services/control-api/src/cloudctl_api/wechat_client.py services/control-api/src/cloudctl_api/wechat_service.py tests/integration/test_wechat_publisher.py tests/integration/test_wechat_publisher_delta.py
.venv/bin/pyright services/control-api/src/cloudctl_api/wechat_client.py services/control-api/src/cloudctl_api/wechat_service.py
.venv/bin/mypy --cache-dir "$PI_SCRATCH_DIR/goal-20260924/f16/acceptance/mypy-cache" services/control-api/src/cloudctl_api/wechat_client.py services/control-api/src/cloudctl_api/wechat_service.py
.venv/bin/python scripts/plan_guard.py docs/current/tasks.json
git diff --check
```

## 执行证据

本轮完整日志根：
`/Users/wangziheng/.pi-desktop/scratch/160ccaac-b6da-40fd-88ad-f957b3ed0b5f/goal-20260924/f16/`

| 检查 | 退出码 | 结果与日志 |
|---|---:|---|
| 定向测试最终轮 | 0 | 58 passed in 7.09s；`acceptance/tests.log`、`tests.xml`、`tests.result.json` |
| Ruff lint | 0 | `acceptance/lint.log`、`lint.result.json` |
| Ruff format | 0 | 4 files already formatted；`acceptance/format.log`、`format.result.json` |
| Pyright | 0 | 0 errors；`review-final/pyright.log`、`pyright.result.json`；此后两个源码文件未变 |
| mypy | 0 | 2 source files no issues；`review-final/mypy.log`、`mypy.result.json`；此后两个源码文件未变 |
| 计划校验 | 0 | `acceptance/plan-guard.json`、`plan-guard.result.json` |
| 差异空白检查 | 0 | `acceptance/diff-check.result.json` |
| 旧解析逻辑反向验证 | 1（预期） | 在 scratch 副本恢复字符串终态判断，7 个数字枚举用例中 6 failed / 1 passed；`acceptance/legacy-state-negative.log`、`legacy-state-negative.result.json`。未改工作区源码 |
| 全仓 make verify | 0 | 使用 `PATH="$PWD/.venv/bin:$PATH" make verify`；Ruff、pnpm lint/typecheck/test、Pyright、mypy、全仓 pytest 全部通过；详见 `quality-20260924.md` 和 scratch `make-verify-20260924-venv-path.log`。|

首轮新增回归曾有 6 项完整 JSON 比较失败，差异仅为内存 SQLite 持久化后的时间戳时区序列化；测试改为在同一持久化边界比较完整记录，并明确断言获胜查询已进入正确终态、resolvedAt 非空、pollCount 为 1。没有删除状态/身份/次数断言。

## 源文件 SHA256

| 文件 | SHA256 |
|---|---|
| `services/control-api/src/cloudctl_api/wechat_client.py` | `a0fbe260c758233c258dc2705b381a0f6807effc35a56e861cf25bc9765fda5c` |
| `services/control-api/src/cloudctl_api/wechat_service.py` | `663a6fab61a57260fee7bf363bf73fd3b5aa0ccdccf4f013a3566201fde37888` |
| `tests/integration/test_wechat_publisher.py` | `f3deb3cb8bb1f868e341328c3d72e882305979d90282d385b82707d07b3a7f4f` |
| `tests/integration/test_wechat_publisher_delta.py` | `a63807590ac5d7ae83531430940ea2b4823c83b1998c629a285457c6805a4028` |

## 官方资料核对

2026-09-24 只读 HTTP 获取以下公开文档，均返回 200；正文保存在 scratch 的 `official-docs/`，不是业务 API 调用。

- 状态查询：`https://developers.weixin.qq.com/doc/service/api/public/api_freepublish_get.html`
  - `publish_id` 为 string，`publish_status` 为 number；0 成功、1 发布中、2 原创失败、3 常规失败、4 审核不通过、5 发布后用户删除、6 发布后系统封禁。
  - 抓取正文 SHA256：`2e59a1b4bb82bd303b1e9c0468e1aef5605770bd777c0d241e71105a3861624f`。
- 提交：`https://developers.weixin.qq.com/doc/service/api/public/api_freepublish_submit.html`
  - 提交成功只表示任务受理，不代表发布完成；必须通过状态查询取得结果。
  - SHA256：`0d871c8c93ff051657fb3bf5eb3243e0a023a6932f3f5357380da3de0f7dd66d`。
- 草稿：`https://developers.weixin.qq.com/doc/service/api/draftbox/draftmanage/api_draft_add.html`
  - 图文封面需永久素材 ID；当前代码只有外部传入 `thumbMediaId`，不能把媒体上传阶段标为完成。
  - SHA256：`c8b8aae08b77df6608dbacd28a060631980af70ce32b8bc17f4ddcecdf344521`。

这些核对不证明具体账号权限、实际接口成功或内容合规。

## 未完成条件及责任边界

- 主控制者：补齐永久素材上传/归属、内容修订与发布目标关联；本轮仅允许假传输实现和测试。
- 主控制者：隔离 PostgreSQL 多连接并发、崩溃恢复验收；SQLite 的可控交错不证明 PostgreSQL 行锁行为。
- 独立代码复核待指定子智能体可用。六类定义及偏好已确认 `deepspace/gpt-5.6-sol / high`，但实际 Task 启动返回 `Model "deepspace/gpt-5.6-sol" is not available for delegation.`。没有成功子智能体复核，也没有回退供应商。
- `BLOCKED_EXTERNAL`：授权测试公众号、接口权限、素材及真实发布授权尚未核验；真实 `draft -> publish -> result` 身份链必须另行明确授权，本轮禁止执行。
- 全仓质量门已通过软件检查：`PATH="$PWD/.venv/bin:$PATH" make verify` exit 0；前端保留 220 条非阻断 warning，未关闭规则。

## 2026-09-24 后续软件收口

### PostgreSQL 隔离 fixture

宿主中文 locale 导致临时 PostgreSQL 16.15 启动时报 `postmaster became multithreaded during startup`。以下五个 fixture 现对 `initdb`、`pg_ctl`、`createdb` 和清理进程显式传递 `LC_ALL=C`、`LANG=C`、`LANGUAGE=C`：

- `tests/integration/test_fleet_claim_recovery.py`
- `tests/integration/test_fleet_cancel_reconcile.py`
- `tests/integration/test_fleet_two_devices.py`
- `tests/integration/test_controlled_fault_matrix.py`
- `tests/integration/test_p14_recipe_versions.py`

定向结果：`99 passed, 7 skipped in 33.89s`。7 个 skip 全部是明确的真实硬件项，不是 PostgreSQL fixture 失败，也没有用 SQLite 替代跨进程行锁证明。

### 全仓软件门禁

`PATH="$PWD/.venv/bin:$PATH" make verify` exit 0：Ruff `619 files already formatted` / `All checks passed!`，Pyright 0，mypy 158 source files no issues，pnpm lint/typecheck/test 通过，全仓 pytest `1151 passed, 11 skipped`，Studio `16 passed`，Web `311 passed`。前端仍有 220 条既有 warning、0 error。

本轮没有真实公众号调用、草稿/素材上传、发布、部署、迁移、APK 安装或手机触屏；F16 的永久素材、账号授权、真实 draft/publish/result 身份链、独立复核和外部验收仍未完成。
