# IM 聚合真机端到端验收 — 2026-09-14（pa-im/20260913.1）

链路：真闲鱼通知 → Companion 无障碍监听 → LRU 去重 → 批量上报 → 云端 im_thread/im_message
→ Web/操作员 API 查看 → 云端回复 → 6 步受控任务 → 手机自动打开会话输入发送 → 屏幕读回。

## 结果

- **消息上报 ✅**：另一真实账号（lucas）发「hello」，通知被捕获，云端建会话 lucas/unread=1，
  消息内容为通知摘要「发来一条新消息」（闲鱼通知不透传正文——平台行为，slice 2 经会话页补全正文）。
- **回复送达 ✅**：操作员 POST :reply {"text":"你好，在的"} → 任务 8a5ead36（第 5 次尝试）
  6 步全部 SUCCEEDED；屏幕读回「你好，在的」在 lucas 会话中（发送侧气泡）；
  截图 sha256 e97ca04232f7acd740e6c685…（未入公开仓库）。任务事件流完整。

## 验收中修复（已推送 535a22b）

1. 锚点真机化：消息 tab=desc 前缀「消息，未读消息数N」（复合串）；聊天输入=desc 前缀「想跟TA说点什么」。
2. tapText 手势化：Flutter 对 ACTION_CLICK 返回 true 但不处理——改为先真实手势点击节点中心。
3. 幂等重放：同 key 重放不再重复插 OUT 消息（500 修复）；每次尝试用回复序号生成新意图键。
4. 启动加 CLEAR_TOP（Flutter 引擎仍会恢复路由——见缺口 1）。

## 已知缺口（slice 2）

1. **应用状态归一化**：闲鱼停在内页（如「我发布的」）时任务起步失败；需导航复位原语
   （本轮人工退回主页后通过）。
2. 通知正文摘要化：需进会话页读取真实内容。
3. 前台聊天页时不产生系统通知（用户操作手机中漏消息场景）。
4. 会话列表仅首屏可见者可 tapText；长列表需滚动定位。

## 门禁

后端 6 用例（sqlite+postgres）；Android 197 单测；服务端 pa-dual-02a10fd 已部署（迁移 0017）。
