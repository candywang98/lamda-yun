# P09 Recipe/service wiring — 2026-09-12

Contract: `contracts/phase1/p09-recipe-service-wiring.md` (`p09-wiring/20260912.1`).
This slice wires signed `commitActionId` to the existing Companion ledger. It does **not** accept G3 or real platform publish.

## Software

- Python focused recipe/ledger tests: 46 passed, 1 skipped (`tests/unit/test_recipe_package.py`, `tests/integration/test_p09_action_ledger.py`). The skip is the existing SQLite lock case.
- Android unit tests: 173 passed, 0 failures/errors/skips (`testDebugUnitTest`).
- `RecipeCommitAdapterTest`: 11 passed.

## Hardware (isolated storage)

OnePlus 9R / Android 14 / `b0644fb5`.

```
./gradlew :app:connectedDebugAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=com.company.cloudctl.companion.automation.RecipeCommitWiringInstrumentationTest
```

Result: `OK (3 tests)` in 3.921s. XML: `TEST-LE2100-recipe-commit-wiring.xml`.

Cases:

1. Marked commit uses the ledger once and stays RECONCILING.
2. Lost grant + reopen never repeats intent or effect.
3. Reported APPLIED still waits for resolutionRevision>0.

The instrumentation uses an isolated cache SQLite directory and a fake ledger. It never opens 闲鱼/小红书/抖音 or the live action API.

## Side effects of the device run

Installing the debug APK for instrumentation dropped Companion from enabled accessibility services. ADB cannot write `secure` settings on this production build. Companion must be re-enabled from system settings before live Recipe work. Crashed services remained empty.

## Not claimed

- Real platform G3.
- Operator auto-reconciliation.
- Platform whitelist expansion.
- Push of the earlier ledger commits (`ce823ad` and parents).
